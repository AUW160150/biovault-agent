# ─── BioVault Agent — Environment Variables ───────────────────────────────────
# Copy this file to .env and fill in values before running locally.
# Never commit .env to version control.

# ── AkashML Inference (REQUIRED) ──────────────────────────────────────────────
# Get your key at: https://chatapi.akash.network
AKASH_API_KEY=your_akash_api_key_here
AKASH_BASE_URL=https://api.akashml.com/v1
AKASH_MODEL=Meta-Llama-3-1-8B-Instruct-FP8

# ── MiniMax Vision (REQUIRED — for vision extraction stage) ───────────────────
# Get your key at: https://platform.minimax.io
MINIMAX_API_KEY=your_minimax_api_key_here
MINIMAX_BASE_URL=https://api.minimax.io/v1

# ── Datadog LLM Observability (OPTIONAL — degrades gracefully) ────────────────
DD_API_KEY=
DD_SITE=us5.datadoghq.com
DD_SERVICE=biovault-agent
DD_ENV=production

# ── Webhook Alerting (OPTIONAL — POST alerts here when critical flags raised) ──
# Accepts any HTTP endpoint: Slack webhook, PagerDuty, custom listener, etc.
WEBHOOK_URL=

# ── Storage Paths ─────────────────────────────────────────────────────────────
# In Docker/Akash these point to the persistent volume mounted at /data.
# For local dev, SQLite and uploads will be created in ./data/ automatically.
DB_PATH=./data/biovault.db
UPLOAD_DIR=./data/uploads

# ── Agent Tuning ──────────────────────────────────────────────────────────────
# How often the agent polls the queue for pending documents (seconds)
POLL_INTERVAL_SECONDS=30
